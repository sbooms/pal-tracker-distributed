import io.pivotal.pal.tracker.eurekaserver.gradlebuild.CfMigrationPlugin
import io.pivotal.pal.tracker.eurekaserver.gradlebuild.LocalMigrationPlugin

subprojects {
    apply plugin: LocalMigrationPlugin
    apply plugin: CfMigrationPlugin
}
